# Reuse student's configuration

< socket.cpf

# but overwrite to a SubscriptionService
# = Subscription service 
SKYCAVE_SUBSCRIPTION_IMPLEMENTATION = cloud.cave.service.RealSubscriptionService
SKYCAVE_SUBSCRIPTIONSERVER = skycave.baerbak.com:7654